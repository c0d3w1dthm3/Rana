{ "id": "SEC-001", "name": "Secret-shaped value in config", "severity": "critical",
  "category": "secrets", "check": "secret_in_config",
  "params": { "extra_patterns": [] },
  "rationale": "A value matching a known credential format is present in config or instructions, where it is exposed to anyone with read access and to the agent's context.",
  "remediation": "Remove the secret, rotate it, and inject it at runtime from a secret manager. Add your org's credential formats via params.extra_patterns.", "maps_to": [] }
